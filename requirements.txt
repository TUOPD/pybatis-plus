# =============================================================================
# pymp —— Pybatis-plus 依赖清单
# 安装：pip install -r requirements.txt
# 说明：本清单按源码实际 import 整理（生成器 CLI 用 argparse、动态 SQL 用内置
#       解析，均不依赖 Jinja2/click，故不再列出）。
# =============================================================================

# -----------------------------------------------------------------------------
# 1. 核心运行依赖（框架必需）
# -----------------------------------------------------------------------------
PyMySQL>=1.0.2          # MySQL 驱动（db_setup 连接池 / Executor 主驱动）
DBUtils>=3.0.0          # 连接池（dbutils.pooled_db.PooledDB）
python-dotenv>=1.0.0    # 读取 .env（init_pymp_with_pool）
Flask>=2.2.0            # init_pymp_with_pool 以 Flask app 作为初始化入口
typing-extensions>=4.5.0; python_version < "3.11"

# -----------------------------------------------------------------------------
# 2. 可选数据库驱动（按需取消注释）
# -----------------------------------------------------------------------------
# psycopg2-binary>=2.9.0        # PostgreSQL（占位符 %s 已适配）

# -----------------------------------------------------------------------------
# 3. 可选：模型层若使用 Pydantic（BaseModel 也兼容 pydantic 模型时）
# -----------------------------------------------------------------------------
# pydantic>=2.0.0

# -----------------------------------------------------------------------------
# 4. 演示工程 / Web 接口（Test/ 目录需要）
# -----------------------------------------------------------------------------
flask-restx>=1.0.0      # Test/api.py 的 REST 测试接口（flask_restx）

# -----------------------------------------------------------------------------
# 5. 开发、测试与代码规范（Dev）
# -----------------------------------------------------------------------------
pytest>=7.3.0
pytest-cov>=4.1.0
ruff>=0.1.0